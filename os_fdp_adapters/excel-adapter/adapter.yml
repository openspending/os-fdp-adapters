adapters:
    - kind: fdp
      rules:
          - "\\.xls$"
          - "\\.xlsx$"
      runner: excel_to_fdp
    - kind: csv
      rules:
          - "\\.xls.csv$"
          - "\\.xlsx.csv$"
      runner: excel_to_fdp
